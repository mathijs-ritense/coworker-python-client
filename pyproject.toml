[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "coworker"
version = "0.1.0"
description = "Client voor de Ritense Coworker API"
requires-python = ">=3.9"
dependencies = [
    "requests",
]
